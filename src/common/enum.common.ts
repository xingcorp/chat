export enum GetOrgChartKind {
    User = "User",
    OrgChart = "OrgChart",
}

export enum GetOrgChartType {
    All = "All",
    WParent = "WParent",
    WChild = "WChild",
    Only = "Only",
}

export enum RequesterRole {
    User = 'User',
    Admin = 'Admin'
}

export enum ActiveStatus {
    Active = 'Active',
    Inactive = 'Inactive'
}

export enum OrdinalCase {
    Desc = 'Desc',
    Asc = 'Asc',
    NameDesc = 'NameDesc',
    NameAsc = 'NameAsc',
    ViewerDesc = 'ViewerDesc',
    ViewerAsc = 'ViewerAsc',
}
