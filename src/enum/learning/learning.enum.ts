export enum LearnProjectStatusEnum {
    ACTIVE = 'ACTIVE',
    DRAFT = 'DRAFT',
    CLOSE = 'CLOSE',
}

export enum ProjectTimeTypeEnum {
    PERIOD = 'PERIOD',
    FIXED = 'FIXED',
}

export enum CourseStatusEnum {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    REPORTED = 'REPORTED',
    ENDED = 'ENDED',
    DRAFT = 'DRAFT',
}

export enum CourseJoinTypeEnum {
    FREE = 'FREE',
    ASSIGN = 'ASSIGN',
}

export enum CourseTrainTypeEnum {
    ONLINE = 'ONLINE',
    OFFLINE = 'OFFLINE',
}

export enum CourseProposerTypeEnum {
    USER = 'USER',
    ADMIN = 'ADMIN',
}

export enum LessonMediaTypeEnum {
    DOCUMENT = 'DOCUMENT',
    VIDEO = 'VIDEO',
    AUDIO = 'AUDIO',
}

export enum CourseTeacherType {
    Internal = 'Internal',
    External = 'External',
}