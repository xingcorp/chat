/// Types of feedback messages.
enum FeedbackType {
  /// Success feedback (green).
  success,

  /// Error feedback (red).
  error,

  /// Warning feedback (orange).
  warning,

  /// Info feedback (blue).
  info,
}
